[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "geomem-env"
version = "0.1.0"
description = "Environment, observation, and task-geometry diagnostics for GeoMem."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
